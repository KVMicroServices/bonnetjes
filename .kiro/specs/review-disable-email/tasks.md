# Tasks

- [x] Create `lib/email/email-translations.ts` that reads `messages/{locale}.json` files, extracts the `ReviewDisableEmail` namespace, maps `failureReason` codes to translated strings, and falls back to `en` for missing locales or keys. Create `lib/email/email-templates.ts` that renders inline-styled HTML email with greeting, body (interpolated reviewId, locationId), failure reason, dispute button linking to `${APP_URL}/dispute?reviewId=${reviewId}`, and footer. Add the `ReviewDisableEmail` namespace with all keys (subject, greeting, body, reasonLabel, failure reason variants, disputeButtonText, footer) to all 8 message files.
- [x] Create `lib/email/email-service.ts` with a `sendReviewDisableEmail(params)` function that validates SMTP env vars are present, creates a lazy Nodemailer SMTP transport singleton, loads translations via `email-translations.ts`, renders HTML via `email-templates.ts`, sends the email, logs success/failure via shared logger, and returns an `EmailResult` object (never throws). Add `nodemailer` and `@types/nodemailer` as dependencies. Add `APP_URL` to `.env.example`.
- [x] Create `lib/review-disable/kiyoh-review-client.ts` with a `resolveReviewerEmail(reviewId, tenantId)` function that calls `authenticateKiyohAdmin()` for the bearer token, fetches `GET ${KIYOH_REVIEW_LIST_URL}?reviewId=${reviewId}&tenantId=${tenantId}&limit=1`, parses the response to extract the `email` field from the first ReviewDto, and returns a `ReviewerEmailResult` (never throws). Add `KIYOH_REVIEW_LIST_URL` to `.env.example` with default `https://www.klantenvertellen.nl/v1/review`.
- [x] Integrate email notification into `lib/queue/review-disable-worker.ts`: after the audit record is marked successful, call `resolveReviewerEmail` with the job's reviewId and tenantId, look up the receipt's `failureReason` from the database, and call `sendReviewDisableEmail` with the resolved email, locale `"en"`, reviewId, locationId, and failureReason. Log warnings on email resolution or send failures without affecting job success.
- [x] Integrate email notification into `app/api/admin/reviews/disable/route.ts`: after a successful `"disable"` action, resolve the reviewer email via `resolveReviewerEmail` using the returned reviewId and the ReceiptSyncState's tenantId, look up the receipt's failureReason, and call `sendReviewDisableEmail`. After a successful `"disable-manual"` action, resolve the reviewer email using the provided reviewId and tenantId, use `"ADMIN_DISABLED"` as the failureReason, and call `sendReviewDisableEmail`. Log warnings on failures without affecting the HTTP response.
- [x] Create `app/dispute/page.tsx` as a public server component that reads `reviewId` from searchParams and renders a minimal localized placeholder page indicating the dispute system is under development, displaying the reviewId if present.
- [x] Create `tests/services/email-service.test.ts` with unit tests covering: SMTP config validation (missing vars return failure), translation loading and fallback to `en`, HTML template rendering with interpolated values and dispute link, `sendReviewDisableEmail` success and transport error paths. Create `tests/services/kiyoh-review-client.test.ts` with unit tests covering: successful email resolution, empty API response handling, network error handling, and missing email field handling. Mock `fetch` and `authenticateKiyohAdmin` at the module level.
