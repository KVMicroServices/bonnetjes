# Tasks

- [x] Delete all moderation, platforms, and automation files: `app/admin/moderation/`, `app/admin/platforms/`, `app/admin/settings/automation/`, `app/api/reviews/` (entire directory), `app/api/admin/automation/` (entire directory), and `lib/automation/` (entire directory); remove the `AutomationWorkflow` model from `prisma/schema.prisma` and create a migration to drop the table
- [x] Update `components/header.tsx` to show only two nav links for authenticated users — "Dashboard" linking to `/admin` (icon: `LayoutDashboard`) and "Settings" linking to `/admin/settings` (icon: `Settings`) — removing the admin-only gating, moderation link, and platforms link; keep sign-out, language selector, and unauthenticated links unchanged
- [x] Replace `app/dashboard/page.tsx` with a server component that calls `redirect("/admin")` from `next/navigation`, removing all existing dashboard code
- [x] In `app/admin/page.tsx`, remove the "users" tab button and its content panel, remove the `ManualDisableForm` component, update the page title/subtitle to use "Dashboard" translations, and add a "Reason" column to the receipt queue table displaying `failureReason` (dash when empty) using the existing `Admin.failureReason` translation key
- [x] Create `app/admin/settings/page.tsx` as a new settings page with admin-only auth check and the user management UI (role dropdown per user, fetching from `/api/admin/users`) extracted from the old admin page users tab, using a new `Settings` translation namespace
- [x] Update all 8 `messages/*.json` files: remove `Moderation`, `Reviews`, `ReviewDisable`, and `Automation` namespaces entirely; update `Header` namespace to remove `adminPanel`/`moderation`/`platforms` and add `settings`; update `Admin.title` to "Dashboard" and remove user-management keys (`userManagement`, `usersCount`, `roleUpdated`, `roleUpdatedDescription`, `roleUpdateFailed`, `roleUpdateFailedDescription`); add a new `Settings` namespace with keys for title, subtitle, userManagement, usersCount, roleUpdated, roleUpdatedDescription, roleUpdateFailed, roleUpdateFailedDescription
- [x] Add unit tests in `tests/` covering: the settings page user management logic (role update fetch calls, auth redirect for non-admins), the dashboard redirect behavior, and that the header renders exactly "Dashboard" and "Settings" links for authenticated users and "Sign In"/"Get Started" for unauthenticated users
