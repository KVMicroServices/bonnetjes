# v26.05.18

Code review for feat/email-notification-on-disable → develop.

**Branch**: feat/email-notification-on-disable → develop
**Date**: 2026-05-18
**Scope**: 36 files changed, 4010 insertions, 21 deletions
**Status**: Done

---

## Standards

### Ternaries (banned: `? :`) — 12 violations

| File | Line | Issue |
|------|------|-------|
| `components/dispute-uploader.tsx` | 167 | `file.type.includes("pdf") ? "pdf" : "image"` |
| `components/dispute-uploader.tsx` | 191 | `error instanceof Error ? error.message : translations("genericError")` |
| `components/dispute-uploader.tsx` | 220 | `error instanceof Error ? error.message : translations("genericError")` |
| `lib/email/email-service.ts` | 171 | `error instanceof Error ? error.message : "Unknown email transport error"` |
| `lib/email/email-translations.ts` | 91 | `isSupportedLocale(locale) ? locale : DEFAULT_LOCALE` |
| `lib/email/email-translations.ts` | 95 | `resolvedLocale === DEFAULT_LOCALE ? null : loadMessagesForLocale(DEFAULT_LOCALE)` |
| `lib/review-disable/kiyoh-review-client.ts` | 55, 81, 110 | `error instanceof Error ? error.message : String(error)` (×3) |
| `app/api/admin/reviews/disable/route.ts` | 89 | `notificationError instanceof Error ? notificationError.message : String(notificationError)` |
| `lib/queue/review-disable-worker.ts` | 101, 126 | `error instanceof Error ? error.message : String(error)` (×2) |

**Fix**: Replace all with if-else blocks.

### Short-circuit tricks (banned: `||` for defaults) — 5 violations

| File | Line | Issue |
|------|------|-------|
| `app/api/admin/reviews/disable/route.ts` | 142 | `receipt?.failureReason \|\| "VERIFICATION_FAILED"` |
| `lib/email/email-service.ts` | 92 | `process.env[APP_URL_VAR] \|\| ""` |
| `lib/services/dispute-service.ts` | 211, 212, 257 | `input.fileType \|\| DEFAULT_FILE_TYPE` and similar (×3) |

**Fix**: Use explicit if-else assignments.

### Nullish coalescing (banned: `??`) — 1 violation

| File | Line | Issue |
|------|------|-------|
| `components/dispute-uploader.tsx` | 273 | `event.target.files?.[0] ?? null` |

**Fix**: Use if-null check.

### Magic values — 3 violations

| File | Line | Issue |
|------|------|-------|
| `lib/s3.ts` | 56 | `expiresIn: 3600` |
| `lib/email/email-service.ts` | 60 | `port === 465` |
| `lib/services/dispute-service.ts` | 212 | `"dispute-receipt"` string literal |

**Fix**: Extract to named constants.

### TypeScript strict mode — 1 violation

| File | Line | Issue |
|------|------|-------|
| `app/dispute/page.tsx` | 49 | `token!` non-null assertion — narrow with if-check instead |

### Passing checks
- Route handlers are thin ✓
- No `console.*` in production code ✓
- No abbreviations or single-letter vars ✓
- No `var`, compound assignments, or chained operations ✓

---

## Security

### 1. Dependency pinning — `@types/nodemailer` uses range version
- **File**: `package.json`
- **Issue**: `"@types/nodemailer": "^8.0.0"` — caret range instead of exact pin
- **Fix**: Pin to `"@types/nodemailer": "8.0.0"`

### 2. Unescaped translation text in email HTML (low risk)
- **File**: `lib/email/email-templates.ts`, lines 103–107
- **Issue**: `translations.intro` injected into HTML without escaping before `{guidelinesLink}` replacement inserts raw HTML
- **Fix**: Escape `translations.intro` first, then replace `{guidelinesLink}` with the pre-built anchor tag

### No issues for:
SQL injection, secrets in code, unvalidated redirects, missing auth, exposed sensitive data, unsafe deserialization, suspicious packages ✓

---

## Localization & Stability

### 1. Race condition in `getOrCreateDisputeUserId` ⚠️
- **File**: `lib/services/dispute-service.ts`, lines 429–453
- **Issue**: Concurrent requests can both pass `findUnique` (both get `null`), then both attempt `create` → unique constraint violation
- **Fix**: Use `upsert` or catch the unique constraint error and retry the find

### 2. Unhandled `response.text()` in non-OK path ⚠️
- **File**: `lib/review-disable/kiyoh-review-client.ts`, line 90
- **Issue**: `await response.text()` outside try-catch when `!response.ok` — body stream error causes unhandled rejection
- **Fix**: Move into existing try block or wrap in its own try-catch

### 3. New env vars not validated at startup ⚠️
- **File**: `.env.example` (new vars: `APP_URL`, `DISPUTE_TOKEN_SECRET`)
- **Issue**: Neither validated at startup; `APP_URL` silently defaults to empty string producing broken links
- **Fix**: Add startup validation that fails/warns if unset in production

### 4. Missing input validation at HTTP boundary ⚠️
- **Files**: `app/api/dispute/upload/route.ts`, `app/api/dispute/verify/route.ts`, `app/api/dispute/request-review/route.ts`
- **Issue**: Request bodies destructured without schema validation — untrusted input used without type/format checks
- **Fix**: Add Zod schemas at route boundaries (consistent with existing pattern in admin route)

### Passing checks
- All user-visible strings use translation system ✓
- All 8 locale files have new namespaces ✓
- No unbounded loops or recursion ✓
- No resource leaks ✓

---

## Verdict

**FAIL** — Two blocking issues require fixes before merge:
1. Race condition in `getOrCreateDisputeUserId` can cause 500 errors under concurrent load
2. Missing input validation on 3 public dispute API routes (untrusted input used without schema validation)

Additionally, 22 standards violations (ternaries, short-circuits, magic values) and 2 stability issues (unhandled rejection, missing env validation) should be addressed.
