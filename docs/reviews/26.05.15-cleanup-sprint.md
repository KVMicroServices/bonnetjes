# v26.05.15

Code review for feat/cleanup-sprint → develop.

**Branch**: feat/cleanup-sprint → develop
**Date**: 2026-05-15
**Scope**: 62 files changed, 4763 insertions, 2907 deletions
**Status**: Done

---

## Standards

15 issues found:

- **8 ternary violations** in production code: `lib/services/receipt-service.ts`, `lib/queue/receipt-worker.ts`, `lib/queue/review-disable-worker.ts`, `lib/receipt-sync/receipt-creator.ts`, `lib/review-disable/kiyoh-auth-client.ts`, `lib/services/ocr-service.ts`, `app/api/receipts/route.ts` — replace with if-else blocks
- **5 magic number violations** in queue config files: 86400, 604800, 2592000, 5000, 1000 used without named constants — extract to descriptive constants
- **1 chained `??` short-circuit** in moderation page — replace with explicit if-null check
- **1 unsafe `as any` cast** in `lib/services/receipt-service.ts:219` — use a proper type assertion or narrow the type

## Security

No issues.

All 8 categories checked and passed:

1. **SQL injection**: All DB access via Prisma ORM with parameterized queries.
2. **XSS**: React JSX escaping used throughout. No `dangerouslySetInnerHTML`.
3. **Secrets in code**: None. Auth client improved to stop logging passwords/OTP codes.
4. **Unvalidated redirects**: Only hardcoded path redirects.
5. **Missing auth checks**: New `/api/locale` route is intentionally public. All admin routes have session + role checks.
6. **Exposed sensitive data**: Admin-only error messages behind auth. No PII exposed to unauthenticated users.
7. **Unsafe deserialization**: External input validated with Zod schemas. OCR JSON parsing has try-catch.
8. **Dependency pinning**: New deps (`bullmq: 5.34.3`, `ioredis: 5.4.1`, `next-intl: 3.25.3`) pinned to exact versions.

## Localization & Stability

### Missing Translations (4 issues)

1. **`components/admin-receipt-card.tsx` ~line 162-169**: Hardcoded English labels in `formatFailureReason` ("Not a Receipt", "Image Unclear", etc.) — wire through `useTranslations("ReceiptCard")`.
2. **`components/admin-receipt-card.tsx` ~line 329, 337**: Hardcoded `"Failure Reason"` and `"Secondary Analysis"` labels — use translation keys.
3. **`app/admin/page.tsx` ~line 966, 975**: Same hardcoded labels in receipt detail modal — use `t("failureReason")` and `t("secondaryAnalysis")` from Admin namespace.
4. **`components/receipt-card.tsx` ~line 244**: Failure reason rendered via `.replace(/_/g, " ")` producing raw English — map codes to translated labels.

### Missing Null Checks at External Boundaries (2 issues)

5. **`app/api/locale/route.ts` line 4-5**: `await request.json()` not wrapped in try-catch. Malformed body throws unhandled error — wrap and return 400.
6. **`lib/review-disable/kiyoh-auth-client.ts` lines 115, 187, 254**: Three `JSON.parse()` calls on external API responses without try-catch. HTML error pages will throw unhelpful errors — wrap and throw descriptive errors.

### Env Var Validation (1 issue)

7. **`scripts/queue-worker.ts`**: `REDIS_URL` not validated at startup. Falls back to `localhost:6379` silently — add startup check or warning log.

---

## Verdict

**WARN** — No blocking issues. 15 standards violations (ternaries, magic numbers) are mechanical fixes. 4 missing translation issues violate the localization rule and should be addressed before merge. Security is clean.
