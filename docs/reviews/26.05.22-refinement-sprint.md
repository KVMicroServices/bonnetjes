# v26.05.22

Code review for feat/refinement-sprint → develop.

**Branch**: feat/refinement-sprint → develop
**Date**: 2026-05-22
**Scope**: 63 files changed, 3953 insertions, 140 deletions
**Status**: Done

---

## Standards

### Banned Pattern: Ternaries (? :)

1. **app/admin/page.tsx:905-907** — Ternary for icon rendering. Fix: if-else block or IIFE.
2. **app/admin/page.tsx:909** — `syncing ? t("syncing") : t("syncNow")`. Fix: if-else.
3. **app/admin/page.tsx:1133-1135** — Ternary for bookmark icon. Fix: IIFE with if-else.
4. **app/admin/page.tsx:1238** — Ternary in title prop. Fix: compute variable above with if-else.
5. **app/api/admin/receipt-sync/trigger/route.ts:21** — `error instanceof Error ? error.message : "Unknown error"`. Fix: if-else.
6. **app/api/dispute/verify/route.ts:261** — `locationId ? locationId : ""`. Fix: if-else.
7. **app/api/dispute/verify/route.ts:262** — `tenantId ? tenantId : 0`. Fix: if-else.
8. **app/api/dispute/verify/route.ts:296** — `failureReason ? failureReason : "VERIFICATION_FAILED"`. Fix: if-else.
9. **lib/email/email-translations.ts:92** — `namespaceName ? namespaceName : TRANSLATION_NAMESPACE`. Fix: if-else.
10. **lib/email/email-translations.ts:169-171** — Namespace ternary. Fix: if-else.
11. **lib/email/email-translations.ts:205-207** — `failureReasonKey` ternary. Fix: if-else.
12. **lib/file-conversion.ts:80, 134, 178** — `error instanceof Error ? error.message : String(error)`. Fix: if-else.
13. **lib/queue/receipt-worker.ts:116** — Same pattern. Fix: if-else.
14. **components/receipt-upload.tsx:360, 363, 439** — Ternaries for className and icons. Fix: compute above with if-else.

### Banned Pattern: Short-circuit tricks (|| as fallback)

15. **components/receipt-upload.tsx:45, 142** — `|| "application/octet-stream"`, `|| resolveContentType()`. Fix: if-else.
16. **lib/services/upload-service.ts:107** — `|| "application/octet-stream"`. Fix: if-else.
17. **lib/services/receipt-service.ts:278, 509** — `|| null`, `|| receipt.cloudStoragePath`. Fix: if-else.
18. **lib/services/notification-service.ts:83** — `|| null`. Fix: if-else.
19. **app/api/dispute/verify/route.ts:117** — `|| result.receipt.id`. Fix: if-else.

### Banned Pattern: Magic numbers

20. **app/admin/page.tsx:274** — `400` inline. Fix: extract `SEARCH_DEBOUNCE_MILLISECONDS`.
21. **app/admin/page.tsx:356** — `3000` inline. Fix: extract `SYNC_INDICATOR_DURATION_MILLISECONDS`.

### Banned Pattern: Magic strings

22. **app/api/dispute/verify/route.ts:296** — `"VERIFICATION_FAILED"` inline. Fix: use named constant.

---

## Security

### 1. Dependency Pinning — Unpinned versions
**File**: `package.json`
**What's wrong**: New deps use `^` ranges: `heic-convert`, `mammoth`, `@types/heic-convert`.
**Fix**: Pin to exact versions (`"2.1.0"`, `"1.8.0"`, `"2.1.0"`).

### 2. Missing Input Validation — Unbounded search parameter
**File**: `app/api/receipts/route.ts`
**What's wrong**: `search` param passed to Prisma `contains` without length validation. DoS vector via expensive LIKE queries.
**Fix**: Add max-length check (e.g., 100 chars) before passing to service layer.

### Non-Issues Confirmed
No SQL injection, XSS, secrets, unvalidated redirects, or missing auth checks. All packages are legitimate and well-known.

---

## Localization & Stability

### Localization

**Issue 1** — `app/api/dispute/verify/route.ts:112-118`
- Hardcoded English strings in `sendNotification()`: `"New dispute received"` and body text.
- Fix: Use translation keys or resolve client-side. (Pre-existing architectural limitation — may defer as tech debt.)

**Issue 2** — `lib/services/comment-service.ts:131/232`
- Hardcoded English notification title — pre-existing but lines modified in this diff.
- Fix: Same architectural issue as above.

All new UI and email translation keys are present in all 8 language files. ✓

### Stability

**Issue 1** — `app/admin/page.tsx:226`
- `searchTimeoutRef` has no cleanup on unmount. Pending timeout could fire on unmounted component.
- Fix: Add `useEffect` cleanup that clears the timeout ref.
- Severity: Low.

No other stability issues. Async error handling, resource cleanup, and external boundary guards are all correct.

---

## General

### 1. Duplicate interfaces
**File**: `lib/email/email-service.ts:26-48`
**What's wrong**: `SendVerifiedEmailParams` and `SendDisputeVerifiedEmailParams` are identical.
**Fix**: Use a single interface or type alias.

### 2. Dead parameters in `buildOcrMessages`
**File**: `lib/services/ocr-service.ts:228-229`
**What's wrong**: `_fileType` and `_originalFilename` are unused but still in the signature. Callers pass values that are silently ignored.
**Fix**: Remove dead parameters from signature and update callers.

### 3. Wasteful API call with empty locationId
**File**: `app/api/dispute/verify/route.ts:248`
**What's wrong**: When `locationId` is null, passes empty string to `resolveLocationLocaleWithFallback`, making a guaranteed-to-fail API call before falling back to "en".
**Fix**: Skip the API call when locationId is empty and use default locale directly.

---

## Verdict

**WARN** — No blocking issues. Unpinned dependencies (security #1) and 22 banned code patterns (standards) should be cleaned up before merge. General issues are minor improvements.
