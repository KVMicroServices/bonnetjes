# v26.05.20

Code review for feat/analytics → develop.

**Branch**: feat/analytics → develop
**Date**: 2026-05-20
**Scope**: 48 files changed, 2991 insertions, 82 deletions
**Status**: Done

---

## Standards

### Banned Patterns: Ternaries

1. **lib/services/analytics-service.ts, line 70-71** — Ternary `totalReceipts > 0 ? Math.round(...) : 0`. Fix: Use if-else block.
2. **lib/services/analytics-service.ts, line 74-75** — Ternary `totalReceipts > 0 ? Math.round(...) : 0`. Fix: Use if-else block.
3. **app/admin/analytics/page.tsx, line 197** — Ternary in className. Fix: Compute with if-else before JSX.
4. **app/admin/analytics/page.tsx, line 362-368** — Chained ternary for loading/empty/data states. Fix: Use if-else or early returns.
5. **app/admin/analytics/page.tsx, line 415** — Ternary in className. Fix: Compute with if-else.
6. **app/admin/analytics/page.tsx, line 655** — Ternary in className. Fix: Compute with if-else.
7. **app/admin/analytics/page.tsx, line 702** — Ternary `categoryKey ? t(categoryKey) : entry.category`. Fix: Use if-else.
8. **app/admin/analytics/page.tsx, line 727-733** — Ternary for button content. Fix: Use if-else.

### Banned Patterns: Short-circuit tricks

9. **app/api/admin/analytics/route.ts, line 57** — `searchParams.get("granularity") || "day"`. Fix: Use if-else to assign default.
10. **app/admin/analytics/page.tsx, line 701** — `badgeColor || defaultBadgeColor`. Fix: Use if-else.

### Banned Patterns: Magic numbers

11. **app/admin/analytics/page.tsx, lines 518/531/535/539/543** — Magic number `8` in `slice(0, 8)`. Fix: Extract `RECEIPT_ID_PREVIEW_LENGTH = 8`.
12. **lib/services/analytics-service.ts, lines 105/108/111/151/152/155/156/159/160** — Magic numbers `60`, `24`, `7`, `1000` for time calculations. Fix: Extract named constants (`MILLISECONDS_PER_SECOND`, `SECONDS_PER_MINUTE`, etc.).
13. **lib/services/analytics-service.ts, lines 71/75** — Magic number `100` for percentage. Fix: Extract `PERCENTAGE_MULTIPLIER = 100`.

### Banned Patterns: Chained operations

14. **lib/services/analytics-service.ts, lines 170-172** — Chained `.filter(...).length`. Fix: Assign filter result to variable, then access `.length`.

## Security

No issues.

## Localization & Stability

### Missing Translations

1. **app/admin/analytics/page.tsx, line ~518** — `buildSummary()` returns hardcoded English strings: `"Receipt "`, `"Role changed to "`, `"Updated: "`. These are user-visible in the table Summary column.
   - **Fix**: Use translation keys via `t()` and add keys to all 8 locale files.

---

## Verdict

**FAIL** — Hardcoded user-visible strings violate the localization requirement (blocking). 14 banned pattern violations require fixes before merge.
