[build]
dockerfilePath = "Dockerfile"
watchPatterns = ["/docs/changelogs/**"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
