services:
  database:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: bonnetjes
      POSTGRES_PASSWORD: bonnetjes
      POSTGRES_DB: bonnetjes
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bonnetjes"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: ${DOCKER_TARGET:-staging}
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://bonnetjes:bonnetjes@database:5432/bonnetjes
      NEXTAUTH_URL: http://localhost:3000
    env_file:
      - .env
    depends_on:
      database:
        condition: service_healthy

  app-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: dependencies
    command: sh -c "npx prisma generate && npm run dev"
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://bonnetjes:bonnetjes@database:5432/bonnetjes
      NEXTAUTH_URL: http://localhost:3000
      WATCHPACK_POLLING: "true"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      database:
        condition: service_healthy

volumes:
  postgres_data:
